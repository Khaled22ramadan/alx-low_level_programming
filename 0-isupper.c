#include "main.h"
/**
 * main -  checks for uppercase character.
 * @x: the number to be checked
 * Return: 1 for upper letter 0 for else
 */
int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
return (0);
}
