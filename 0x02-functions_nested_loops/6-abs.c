#include "main.h"
#include <stdio.h>
/**
 *_abs - A.  function that computes the absolute value of an integer.
 *@n: variable to checked.
 *Return: 0 (Success)
 */
int _abs(int n)
{
	int x;

	x = _abs(n);
	_putchar("%d\n", x);
return (0);
}
