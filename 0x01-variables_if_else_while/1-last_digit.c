#include <stdio.h>
#include <stdlio.h>

/**
 *main . return alphabets
 *
 * Return Always 0 (success)
 */

int main (void)
{
	int ch = 'a';
	while (ch <= '2' )
	{
		putchar(ch);
		ch++;
	}
	putchar ('\n');
	return (0);
}
